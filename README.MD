# 前端自动化部署

旧版本

```json
{
  "name": "front-deploy",
  "version": "0.0.0",
  "scripts": {
    "build-deploy": "node deploy.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "compressing": "1.9.0",
    "ora": "1.2.0",
    "ssh2": "^0.6.2"
  }
}

```